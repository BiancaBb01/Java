package org.example;

/**
 * The Person record represents a person with a name and an ID.
 * It is a simple immutable data structure for storing person information.
 */
public record Person(String name, String id) {
}
