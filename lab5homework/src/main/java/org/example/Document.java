package org.example;

/**
 * The Document record represents a document with a name.
 * It is a simple immutable data structure for storing document information.
 */
public record Document(String name) {
}
